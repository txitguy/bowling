/**
 * Constants
 *
 * These constants can be accessed throughout the entire
 * application.
 */

'use strict';

let AppSettings = {
	defaultTemplate: 'dev',
	defaultTheme: 'base'
};

module.exports = AppSettings;